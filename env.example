# App Environment (controls which API URL to use)
# Set this to: development, staging, or production
NEXT_PUBLIC_APP_ENV=development

# API URLs - Environment Specific
NEXT_PUBLIC_DEV_API_URL=http://localhost:8000/api
NEXT_PUBLIC_STAGING_API_URL=https://staging-api.rukapay.com/api
NEXT_PUBLIC_PRODUCTION_API_URL=https://api.rukapay.com/api

# Legacy fallback (if NEXT_PUBLIC_APP_ENV specific URL not set)
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# NextAuth
NEXTAUTH_SECRET=your-secret-key-here
NEXTAUTH_URL=http://localhost:3000

# How It Works:
# NEXT_PUBLIC_APP_ENV=development → uses NEXT_PUBLIC_DEV_API_URL
# NEXT_PUBLIC_APP_ENV=staging → uses NEXT_PUBLIC_STAGING_API_URL
# NEXT_PUBLIC_APP_ENV=production → uses NEXT_PUBLIC_PRODUCTION_API_URL

# Example Usage:
# For local development: NEXT_PUBLIC_APP_ENV=development
# For staging: NEXT_PUBLIC_APP_ENV=staging
# For production: NEXT_PUBLIC_APP_ENV=production
